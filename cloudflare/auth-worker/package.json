{
  "name": "easyhouse-auth-worker",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute easyhouse-auth --remote --file=./schema.sql",
    "db:migrate:local": "wrangler d1 execute easyhouse-auth --local --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260918.1",
    "typescript": "^5.7.3",
    "wrangler": "^4.14.0"
  }
}
